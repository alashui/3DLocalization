%This will act as a user manual. It will document how to use the prelocalization programs to generate the necessary information and the actual localization process. This is different than the code documentation in this doesn't cover our data structures or algorithms, it just tells the user how to install everything, build everything, and everything else to get connected to and localize their robots.
